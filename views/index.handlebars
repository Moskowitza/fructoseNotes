<header class="font">HelloFructose</header>
<nav>
    <div class="nav-wrapper">
        <a href="#!" id="font" class="brand-logo">Hello Fructose</a>
        <a href="#" data-target="mobile-demo" class="sidenav-trigger">
            <i class="material-icons">menu</i>
        </a>
        <ul class="right hide-on-med-and-down">
            <li id="scrape">
                <a href="#">Scrape</a>
            </li>
            <li>
                <a href="https://github.com/Moskowitza/fructoseNotes">Github</a>
            </li>
            <li>
                <a href="http://www.hifructose.com">Hi Fructose</a>
            </li>
        </ul>
    </div>
</nav>
<div class="container">
    {{#each articles}}
    <div class="row">
        {{!-- Show my articles --}} {{!-- For every article in the DB --}}
        <div id="articles" class="col l4" data-id={{this._id}}>
            <h4> {{this.title}}</h4>
            <img src={{this.imgLink}}> {{this.bodyText}}
            <a href="{{this.link}}">Link</a>
            <button class="delete" data-id="{{this.id}}">Remove Article</button>
        </div>
        <div id="notes" class="col l4">
            {{#each note}}
            <b>{{this.title}}</b>
            {{this.body}} {{!-- Delete Button needs to be hooked up --}}
            <button class="deleteNote" data-id="{{this.id}}">Remove Note</button>
            <hr> 
            {{/each}}
        </div>
        <div id="addNote" class="col l4" data-id={{this._id}}>
            <h6> {{this.title}} </h6>
            <input id='titleinput' name='title'>
            <textarea id='bodyinput' name='body'>textarea</textarea>
            <button data-id={{this._id}} class="savenote">Save Note</button>
        </div>
    </div>
    <hr>
    {{/each}}
</div>
</div>